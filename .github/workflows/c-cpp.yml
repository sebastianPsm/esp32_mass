name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: ESP8266-RTOS-SDK (IDF Style) Build
      uses: nsfilho/esp8266-rtos-sdk@v2
      with:
        # Arguments to pass for make
        makearg: # optional, default is defconfig all size-components
